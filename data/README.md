# Data directories

Paid runs write append-only records, a shared project cost journal, and deterministic manifests to `data/raw/`. Analysis output belongs in `data/processed/`. Both generated directories are ignored by Git until their contents have been inspected and a deliberate publication decision has been made.
